/*
const person = {
  age: 26,
  location: {
    city: 'New York',
    temp: '90'
  }
};

const { name = 'Matthew', age } = person;

const { city, temp } = person.location;

console.log(`${name} is ${age}`)

console.log(`${city} is ${temp}`);
*/

//ARRAY

const address = ['1299 S Juniper Street', 'Philly', 'Penn', '19147'];

const measurements = ['38', '24', '44']

const [tits, waist, ass] = measurements;

console.log(`Her ass is ${ass} inches of ass.`)
